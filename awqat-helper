#!/bin/sh
set -eu
plugin_dir=$(CDPATH='' cd -- "$(dirname -- "$0")" && pwd)
if [ -x "$plugin_dir/bin/awqat-core" ]; then
  exec "$plugin_dir/bin/awqat-core" "$@"
fi
printf '%s\n' '{"ok":false,"error":"Awqat needs its Rust helper. Run build.sh in the Awqat plugin folder, then refresh."}'
